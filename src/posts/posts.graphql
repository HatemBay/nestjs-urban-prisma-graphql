scalar DateTime

type Post {
  id: Int!
  author: User
  authorId: Int!
  title: String!
  content: String!
  published: Boolean
  isU18: Boolean
  likesCount: Int
  dislikesCount: Int
  createdAt: DateTime!
  updatedAt: DateTime!
}

input CreatePostInput {
  title: String!
  content: String!
  isU18: Boolean
}

input UpdatePostInput {
  title: String
  content: String
}

input FindPostInput {
  id: Int!
}

input OrderByParams {
  field: String
  direction: String
}

input PaginationParams {
  page: Int
  take: Int
  filter: String
}

type Query {
  posts(orderBy: OrderByParams, pagination: PaginationParams): [Post]!
  post(id: Int!): Post
}

type Mutation {
  createPost(createPostInput: CreatePostInput!): Post!
  updatePost(
    findPostInput: FindPostInput!
    updatePostInput: UpdatePostInput!
  ): Post!
  removePost(id: Int!): Post
}
